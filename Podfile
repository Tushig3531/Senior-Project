# Define the minimum iOS version for your app
platform :ios, '16.0'

target 'BaseballApp' do
  # Use dynamic frameworks (required for Firebase)
  use_frameworks!
  use_modular_headers!


  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Storage'

end
